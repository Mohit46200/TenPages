# Base URL of the backend API.
# (The Razorpay checkout key is fetched from the backend automatically when
# a payment starts — there is nothing Razorpay-related to configure here.)
VITE_API_URL=http://localhost:5000/api
